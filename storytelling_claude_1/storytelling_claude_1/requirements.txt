# run.py needs only these:
PyYAML>=6.0
python-dotenv>=1.0      # optional: lets run.py read a .env file
openai>=1.0             # only if running the openai provider
anthropic>=0.40         # only if running the anthropic provider
google-genai>=1.0       # only if running the gemini provider

# analyze.py (analysis-time only — never loaded during a collection run):
numpy>=1.24
scikit-learn>=1.3
# For best results, semantic clustering. If absent, analyze.py automatically
# falls back to TF-IDF and tells you. The default model is small (~80MB); it
# pulls in torch (~hundreds of MB), the bulk of the install footprint.
sentence-transformers>=2.2
